export default function MultilineLiteralConsumer() {
  const styles = {
    fontSize: '28px',
    lineHeight: 1.2,
    padding:
      '12px 16px',
    gap:
      12,
    color:
      'rgba(15, 23, 42, 0.84)',
    borderRadius:
      14,
    transition:
      'opacity 0.3s ease',
  }

  return <div style={styles}>Literal consumer</div>
}
